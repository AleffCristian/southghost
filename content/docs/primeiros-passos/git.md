---
title: "Git"
weight: 2
---

O fluxo básico para salvar uma alteração:

```powershell
git status
git add .
git commit -m "feat: describe the change"
git push
```

Antes de cada commit, rode `git status` e entenda o que será salvo.
